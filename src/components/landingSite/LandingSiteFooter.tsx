import type { UiStrings } from '@/i18n/ui/types';

/** Native: no marketing footer in this shell. */
export function LandingSiteFooter(_props: { landing: UiStrings['landing'] }): null {
  return null;
}
