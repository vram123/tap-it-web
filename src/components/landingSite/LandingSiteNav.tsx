import type { UiStrings } from '@/i18n/ui/types';

/** Native: landing uses in-scroll hero only; top chrome is web-only. */
export function LandingSiteNav(_props: { landing: UiStrings['landing'] }): null {
  return null;
}
